server:
  type: simple
  applicationContextPath: /
  connector:
    type: http
    port: ${YERBIE_PORT:-5865}

logging:
  level: INFO

redis:
  host: ${REDIS_HOST:-localhost}
  port: ${REDIS_PORT:-6379}
